import { TournamentRequestsInterceptor } from './tournament-requests-interceptor';

describe('TournamentRequestsInterceptor', () => {
  it('should create an instance', () => {
    expect(new TournamentRequestsInterceptor()).toBeTruthy();
  });
});
